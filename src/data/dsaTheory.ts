export type PatternTheory = {
  idea: string
  recognize: string[]
  steps: string[]
  invariant: string
  complexity: string
  pitfalls: string[]
}

export type PatternBlueprint = {
  state: string
  template: string[]
  adapt: string[]
  trace: string[]
}

const theories = {
  linkedList: {
    idea: 'A linked list stores order through references rather than contiguous indices. Most solutions are careful pointer rewiring or one-pass traversal.',
    recognize: ['The input is a chain of nodes.', 'You must insert, delete, reverse, merge, or find a position without array indexing.'],
    steps: ['Draw three consecutive nodes.', 'Name every pointer before changing it.', 'Save the next node before overwriting a link.', 'Handle empty, one-node, head, and tail cases.'],
    invariant: 'The already-processed section has the intended links, and the unprocessed section remains reachable.',
    complexity: 'Traversal is usually O(n) time and O(1) auxiliary space; direct head operations are O(1).',
    pitfalls: ['Losing the rest of the list.', 'Dereferencing None.', 'Forgetting that changing head may require a returned head.'],
  },
  fastSlow: {
    idea: 'Move two pointers at different speeds or from different starting offsets to reveal structure without extra memory.',
    recognize: ['Middle, cycle, cycle length, nth node from end, or linked-list intersection.', 'A brute-force solution stores visited nodes or computes length first.'],
    steps: ['Choose each pointer’s speed or initial gap.', 'State when the pointers can meet.', 'Stop before an unsafe next access.', 'Prove what their distance means at termination.'],
    invariant: 'The relative distance between pointers represents the quantity being measured.',
    complexity: 'Usually O(n) time and O(1) space.',
    pitfalls: ['Wrong loop condition.', 'Moving a pointer before checking it.', 'Applying the cycle proof without resetting pointers correctly.'],
  },
  fundamentals: {
    idea: 'Start with direct simulation and make the representation, operations, and complexity explicit before optimizing.',
    recognize: ['The task teaches syntax, data-structure operations, or basic iteration.', 'Constraints are small and a straightforward pass is sufficient.'],
    steps: ['Write the input and output types.', 'Simulate the required operation directly.', 'Trace one normal and one boundary example.', 'State time and space costs.'],
    invariant: 'After processing index i, all elements through i satisfy the required basic operation.',
    complexity: 'Commonly O(n) time; space depends on whether a new result is built.',
    pitfalls: ['Off-by-one loops.', 'Mutating while iterating unexpectedly.', 'Claiming O(1) space when the output or helper structure grows.'],
  },
  array: {
    idea: 'Arrays provide constant-time indexing. Efficient solutions usually maintain a processed region, running state, or carefully chosen indices.',
    recognize: ['The problem asks about subarrays, rearrangement, leaders, rotations, or element positions.', 'A nested-loop solution repeats work across overlapping ranges.'],
    steps: ['Define what each index or running variable means.', 'Process left to right unless ordering suggests otherwise.', 'Update the answer only after the state is valid.', 'Test duplicates, negatives, and length 0 or 1.'],
    invariant: 'Everything before the scan boundary has been processed correctly.',
    complexity: 'A single scan is O(n); sorting first is O(n log n).',
    pitfalls: ['Confusing subsequence with subarray.', 'Overwriting values still needed later.', 'Ignoring duplicate values.'],
  },
  kadane: {
    idea: 'Keep the best subarray ending at the current position and decide whether to extend the old segment or start fresh.',
    recognize: ['Maximum or minimum contiguous subarray.', 'The result ending at i depends only on the result ending at i - 1.'],
    steps: ['Define current as the best sum ending here.', 'Choose between starting at the current value and extending.', 'Update the global best.', 'Initialize from the first value for all-negative arrays.'],
    invariant: 'current is the best valid answer that must end at the current index.',
    complexity: 'O(n) time and O(1) space.',
    pitfalls: ['Resetting to zero when all values may be negative.', 'Returning current instead of the global best.'],
  },
  matrix: {
    idea: 'Treat rows, columns, diagonals, and boundaries as coordinates with explicit movement rules.',
    recognize: ['Spiral order, rotation, zeroing, search, or traversal of a grid.', 'The operation depends on neighboring cells or matrix boundaries.'],
    steps: ['Write row and column bounds.', 'Choose traversal order.', 'Guard each boundary before a second direction pass.', 'Decide whether updates must be in-place.'],
    invariant: 'Visited cells are complete and the active rectangle contains every unprocessed cell.',
    complexity: 'Most full traversals are O(rows × columns).',
    pitfalls: ['Mixing row and column lengths.', 'Processing a row or column twice when bounds cross.', 'Using changed cells as markers accidentally.'],
  },
  string: {
    idea: 'Strings are arrays of characters; use indices, frequency state, or a constructed result while respecting immutability.',
    recognize: ['Reversal, palindrome, parsing, transformation, or character comparison.', 'Only relative character positions matter.'],
    steps: ['Normalize only if the prompt permits it.', 'Choose indices or build an output buffer.', 'State what portion already matches the goal.', 'Test empty strings and repeated characters.'],
    invariant: 'The processed prefix or suffix already has its final correct form.',
    complexity: 'Usually O(n) time; constructing a result commonly needs O(n) space.',
    pitfalls: ['Repeated immutable concatenation causing O(n²).', 'Incorrect character normalization.', 'Ignoring Unicode or case rules stated by the prompt.'],
  },
  twoPointers: {
    idea: 'Two indices move according to a rule that eliminates impossible candidates without checking every pair.',
    recognize: ['Pair sum, sorted data, palindrome, partitioning, or opposite-end comparison.', 'A brute-force solution checks O(n²) pairs.'],
    steps: ['Define what lies outside each pointer.', 'Compare the current pair.', 'Move only the pointer whose movement can improve the condition.', 'Prove discarded candidates cannot work.'],
    invariant: 'All candidates outside the pointer range have been safely eliminated.',
    complexity: 'O(n) after sorting; O(n log n) if sorting is required.',
    pitfalls: ['Using the method without sorted or monotonic structure.', 'Moving both pointers without proof.', 'Losing original indices after sorting.'],
  },
  stack: {
    idea: 'A stack is useful when the newest unresolved item must be handled first.',
    recognize: ['Nested expressions, undo behavior, nearest element, or matching delimiters.', 'The answer depends on the most recent unmatched item.'],
    steps: ['Define exactly what each stack entry represents.', 'State when entries are pushed and popped.', 'Process the current token.', 'Handle anything left in the stack at the end.'],
    invariant: 'The stack contains precisely the unresolved items in useful order.',
    complexity: 'Usually O(n) time and O(n) space.',
    pitfalls: ['Reading the top of an empty stack.', 'Storing values when indices are needed.', 'Forgetting remaining entries.'],
  },
  monotonic: {
    idea: 'Maintain a stack or deque in increasing or decreasing order so dominated candidates are removed once.',
    recognize: ['Next greater/smaller, stock span, histogram, or window maximum.', 'For every item, you need the nearest better candidate.'],
    steps: ['Choose increasing or decreasing order.', 'Pop while the new item dominates the top.', 'Resolve answers during pops.', 'Push the current index and finish unresolved entries.'],
    invariant: 'Entries remain monotonic and every removed entry has received its final answer.',
    complexity: 'O(n) time because each item is pushed and popped at most once; O(n) space.',
    pitfalls: ['Wrong strict versus non-strict comparison.', 'Using values instead of indices.', 'Misreading whether the answer is left or right.'],
  },
  hashing: {
    idea: 'A set or map trades memory for near-constant-time membership and frequency queries.',
    recognize: ['Duplicates, counts, complements, grouping, or previously seen values.', 'The brute force repeatedly searches earlier elements.'],
    steps: ['Choose set, frequency map, or value-to-index map.', 'Decide whether to query before or after inserting.', 'Update counts consistently.', 'Check duplicate and missing-key behavior.'],
    invariant: 'The map summarizes exactly the processed portion of the input.',
    complexity: 'Average O(n) time and O(n) space.',
    pitfalls: ['Inserting before querying when the same element cannot be reused.', 'Overwriting a needed index.', 'Assuming deterministic ordering from a hash map.'],
  },
  prefix: {
    idea: 'A prefix value summarizes everything before an index, turning range calculations into subtraction or map lookups.',
    recognize: ['Many range sums, subarray count/sum, or cumulative XOR.', 'The same prefixes are recomputed repeatedly.'],
    steps: ['Define prefix 0 as the empty prefix.', 'Update the running prefix.', 'Query the required earlier prefix.', 'Store the current prefix with the correct frequency.'],
    invariant: 'prefix represents exactly the input before the current boundary.',
    complexity: 'O(n) time and O(n) space for counting; range queries become O(1) after preprocessing.',
    pitfalls: ['Missing the empty prefix.', 'Wrong query-before-store order.', 'Using sliding window when negative values break monotonicity.'],
  },
  slidingWindow: {
    idea: 'Maintain a contiguous range and update its state incrementally as the right boundary expands and the left boundary shrinks.',
    recognize: ['Longest/shortest valid substring or subarray.', 'The validity condition changes predictably when boundaries move.'],
    steps: ['Define the window and its state.', 'Expand right.', 'Shrink left until valid or optimal.', 'Update the answer at the correct moment.'],
    invariant: 'The maintained state describes exactly the current [left, right] window.',
    complexity: 'Usually O(n) time because each boundary moves forward at most n times; O(k) state.',
    pitfalls: ['Updating the answer while invalid.', 'Forgetting to remove the left item.', 'Using a variable window when the condition is not monotonic.'],
  },
  trie: {
    idea: 'A trie shares prefixes across keys, making lookup proportional to key length instead of the number of stored keys.',
    recognize: ['Prefix search, dictionary words, autocomplete, or maximum XOR by bits.', 'Many strings or numbers share prefixes.'],
    steps: ['Choose character or bit edges.', 'Walk from the root one symbol at a time.', 'Create missing nodes during insertion.', 'Distinguish full-word markers from prefixes.'],
    invariant: 'The path from the root spells exactly the processed prefix.',
    complexity: 'Insert and search are O(length of key), with space proportional to created trie nodes.',
    pitfalls: ['Treating a prefix as a complete word.', 'Large alphabet memory use.', 'Choosing the same bit instead of the opposite bit for maximum XOR.'],
  },
  tree: {
    idea: 'Trees are recursive: solve the current node, then solve independent child subtrees and combine their results.',
    recognize: ['Traversal, height, path, view, ancestor, construction, or serialization.', 'The input has parent-child hierarchy without cycles.'],
    steps: ['Define the return value for one subtree.', 'Write the empty-node base case.', 'Choose preorder, inorder, postorder, or BFS based on when information is needed.', 'Combine child results at the parent.'],
    invariant: 'Each completed subtree has returned exactly the information promised by the recursive definition.',
    complexity: 'Most traversals are O(n) time; recursion uses O(height) space.',
    pitfalls: ['Mixing node count and edge count for height.', 'Global state leaking between calls.', 'O(n²) repeated subtree work.'],
  },
  heap: {
    idea: 'A heap exposes the smallest or largest priority quickly while maintaining only parent-child order.',
    recognize: ['Repeated min/max extraction, top k, merging sorted streams, or scheduling by priority.', 'Sorting everything is wasteful because only the next extreme is needed.'],
    steps: ['Choose min-heap or max-heap.', 'Define the heap entry and tie-breaker.', 'Push candidates as they become eligible.', 'Pop only when the next priority is needed.'],
    invariant: 'The root is the best currently eligible candidate.',
    complexity: 'Push and pop are O(log n); peek is O(1); building a heap is O(n).',
    pitfalls: ['Claiming the whole heap is sorted.', 'Using the wrong heap direction.', 'Forgetting stale entries in lazy-update designs.'],
  },
  intervals: {
    idea: 'Sort intervals by the boundary that makes overlap decisions deterministic, then scan once.',
    recognize: ['Merge, insert, meetings, scheduling, or overlapping ranges.', 'Pairwise overlap checking leads to O(n²).'],
    steps: ['Choose sorting by start or end based on the goal.', 'Keep the current merged or selected interval.', 'Compare the next boundary.', 'Merge, select, or skip with a stated rule.'],
    invariant: 'All intervals before the scan are fully resolved and the active interval summarizes unresolved overlap.',
    complexity: 'O(n log n) time for sorting and O(n) output space in common variants.',
    pitfalls: ['Wrong endpoint inclusivity.', 'Sorting by the wrong boundary.', 'Failing to append the final interval.'],
  },
  sorting: {
    idea: 'Sorting imposes order so local comparisons can replace broad searching; learn both the algorithm and when sorting unlocks another pattern.',
    recognize: ['Order statistics, duplicate grouping, interval processing, or explicitly implementing a sort.', 'The relative order makes later decisions monotonic.'],
    steps: ['Choose a comparison key.', 'Know whether stability matters.', 'Trace swaps or merges on a tiny input.', 'Include sorting cost in final complexity.'],
    invariant: 'The completed region is in final sorted order, or each merged run is sorted.',
    complexity: 'Efficient comparison sorts are O(n log n); counting-based methods depend on value range.',
    pitfalls: ['Forgetting mutation.', 'Claiming linear total time after sorting.', 'Using unstable sorting when equal-item order matters.'],
  },
  graph: {
    idea: 'Represent relationships with adjacency lists and separate discovered nodes from unexplored edges.',
    recognize: ['Arbitrary connections, routes, components, dependencies, or transformations between states.', 'The structure may contain cycles or disconnected components.'],
    steps: ['Build the adjacency representation.', 'Choose BFS, DFS, or a specialized algorithm.', 'Mark nodes at discovery time.', 'Restart from unvisited nodes if components matter.'],
    invariant: 'Every discovered node is recorded once, and the frontier contains remaining exploration work.',
    complexity: 'A complete BFS or DFS is O(V + E) time and O(V) space.',
    pitfalls: ['Treating a directed edge as undirected.', 'Marking visited too late.', 'Ignoring disconnected nodes.'],
  },
  topo: {
    idea: 'Topological order exists only for directed acyclic graphs and places every prerequisite before what depends on it.',
    recognize: ['Course prerequisites, build order, alien dictionary, or dependency scheduling.', 'Edges mean “must happen before.”'],
    steps: ['Compute indegrees or DFS states.', 'Begin with zero-indegree nodes.', 'Remove outgoing edges and enqueue newly available nodes.', 'Detect a cycle if fewer than V nodes are ordered.'],
    invariant: 'Every emitted node currently has no unmet prerequisite.',
    complexity: 'O(V + E) time and O(V) space.',
    pitfalls: ['Using it on an undirected graph.', 'Not detecting cycles.', 'Reversing edge direction.'],
  },
  shortestPath: {
    idea: 'Shortest-path algorithms repeatedly finalize or relax the best-known distance using assumptions about edge weights.',
    recognize: ['Minimum cost, distance, time, transformations, or weighted routes.', 'The graph asks for an optimal path rather than simple reachability.'],
    steps: ['Identify edge-weight constraints.', 'Choose BFS, Dijkstra, Bellman-Ford, or DAG relaxation.', 'Initialize source distance to zero and others to infinity.', 'Relax edges and ignore stale work.'],
    invariant: 'Distances are upper bounds; finalized distances are proven optimal under the chosen algorithm’s assumptions.',
    complexity: 'BFS is O(V + E); heap Dijkstra is O((V + E) log V).',
    pitfalls: ['Dijkstra with negative edges.', 'Forgetting stale heap entries.', 'Overflow from adding to infinity.'],
  },
  unionFind: {
    idea: 'Disjoint-set union tracks connected components under repeated merges using a representative for each set.',
    recognize: ['Dynamic connectivity, redundant edge, Kruskal MST, or grouping equivalence.', 'You repeatedly ask whether two items belong to the same component.'],
    steps: ['Initialize each node as its own parent.', 'Find representatives with path compression.', 'Union by rank or size.', 'Use equal representatives to detect existing connectivity.'],
    invariant: 'Nodes in one component resolve to the same representative.',
    complexity: 'Nearly O(1) amortized per operation, formally O(α(n)).',
    pitfalls: ['Unioning raw nodes instead of roots.', 'Updating rank incorrectly.', 'Forgetting path compression.'],
  },
  backtracking: {
    idea: 'Backtracking explores a decision tree by choosing an option, recursing, and undoing the choice exactly.',
    recognize: ['Generate all arrangements, satisfy constraints, maze, N-Queens, Sudoku, or word search.', 'You must enumerate choices while pruning invalid partial states.'],
    steps: ['Define the recursive state and completion condition.', 'List valid choices.', 'Apply one choice.', 'Recurse, then undo before the next choice.'],
    invariant: 'At function entry, the partial state is valid and represents exactly the decisions on the current path.',
    complexity: 'Usually exponential or factorial; pruning reduces explored states but not the broad worst case.',
    pitfalls: ['Forgetting to undo.', 'Appending a mutable object without copying.', 'Checking constraints too late.'],
  },
  recursion: {
    idea: 'Define the solution in terms of a strictly smaller instance and trust that recursive contract.',
    recognize: ['Natural nested structure, repeated smaller subproblem, or generate/take-not-take decisions.', 'The iterative control flow is harder to express than the recursive relationship.'],
    steps: ['State what the function returns.', 'Write the smallest valid base case.', 'Make progress toward it.', 'Combine returned results without changing the contract.'],
    invariant: 'Every call receives a smaller valid problem and returns the promised result for that problem.',
    complexity: 'Count calls for time and maximum call depth for auxiliary space.',
    pitfalls: ['No progress toward base case.', 'Exponential repeated calls.', 'Confusing output storage with call-stack space.'],
  },
  subsets: {
    idea: 'For every element, branch between including it and excluding it, or iterate choices from a start index.',
    recognize: ['Subsets, subsequences, combinations, combination sum, or power set.', 'Each item may be chosen under a small set of rules.'],
    steps: ['Define the current partial answer and next index.', 'Record an answer at the correct recursion point.', 'Choose, recurse, and undo.', 'Sort and skip equal choices when unique results are required.'],
    invariant: 'The path contains exactly the choices made before the current index.',
    complexity: 'O(2ⁿ) states for subsets, plus output-copy cost.',
    pitfalls: ['Confusing subsequence with subarray.', 'Duplicate outputs.', 'Reusing an element when reuse is forbidden.'],
  },
  binarySearch: {
    idea: 'Binary search uses a monotonic condition to discard half of an ordered candidate space after each check.',
    recognize: ['Sorted lookup, first/last occurrence, rotated order, or a yes/no feasibility threshold.', 'A condition changes from false to true at a boundary.'],
    steps: ['Define the inclusive or half-open search interval.', 'Compute mid safely.', 'State which side remains possible after the condition.', 'Return the boundary or verify the final candidate.'],
    invariant: 'Every possible answer remains inside the current search range.',
    complexity: 'O(log n) checks; total cost is O(log range × cost of one feasibility check) for answer search.',
    pitfalls: ['Infinite loops from wrong boundary updates.', 'Returning any match when first/last is required.', 'Searching on an answer without proving monotonicity.'],
  },
  dp: {
    idea: 'Dynamic programming stores answers to overlapping subproblems whose optimal result can be built from smaller states.',
    recognize: ['Count ways, minimum/maximum cost, longest structure, or take/skip choices.', 'Plain recursion repeats the same state.'],
    steps: ['Write the state meaning in one sentence.', 'Derive the transition from previous states.', 'Set base cases.', 'Choose memoization or a dependency-safe table order.'],
    invariant: 'Every computed state equals the correct answer for its exact state definition.',
    complexity: 'Number of distinct states × work per transition; space is stored states plus recursion depth.',
    pitfalls: ['An ambiguous state definition.', 'Wrong iteration order.', 'Optimizing space before the transition is correct.'],
  },
  greedy: {
    idea: 'A greedy algorithm commits to the locally best choice only when a proof shows an optimal solution can include that choice.',
    recognize: ['Scheduling, minimum resources, reachability, matching, or repeatedly choosing an extreme.', 'The problem has an exchange argument or stays-ahead property.'],
    steps: ['Identify the candidate local choice.', 'Sort if it makes the choice visible.', 'State what future flexibility the choice preserves.', 'Prove or test whether exchanging another choice can never hurt.'],
    invariant: 'The decisions made so far can still be extended to an optimal solution.',
    complexity: 'Often O(n log n) due to sorting, followed by O(n) scanning.',
    pitfalls: ['Assuming “looks best now” is proof.', 'Using the wrong sorting key.', 'Missing counterexamples with ties or negative values.'],
  },
  bit: {
    idea: 'Bit manipulation represents flags and powers of two compactly; XOR is especially useful because equal values cancel.',
    recognize: ['Unique element, subset mask, power of two, bit count, or integer operations without arithmetic helpers.', 'Values are constrained by binary representation.'],
    steps: ['Write a small value in binary.', 'Choose AND, OR, XOR, shift, or a mask.', 'State what each bit means.', 'Check signed-number and bit-width assumptions.'],
    invariant: 'Each bit position independently represents the intended state or parity.',
    complexity: 'Often O(number of machine bits) per value, commonly treated as O(1), or O(n) across input.',
    pitfalls: ['Operator-precedence mistakes.', 'Infinite right shifts for negatives.', 'Confusing XOR with exponentiation.'],
  },
  math: {
    idea: 'Reduce the problem to number properties such as divisibility, gcd, modular arithmetic, primes, or combinatorial counting.',
    recognize: ['Factors, primes, gcd/lcm, modular answers, powers, or digit behavior.', 'Simulation is much larger than the numeric structure requires.'],
    steps: ['Write the relevant identity.', 'Reduce values early when using modulo.', 'Use Euclid, sieve, fast exponentiation, or factorization as appropriate.', 'Check zero, one, negatives, and overflow.'],
    invariant: 'Each transformation preserves the mathematical quantity being computed.',
    complexity: 'Varies by method: gcd O(log n), sieve O(n log log n), fast power O(log exponent).',
    pitfalls: ['Applying modulo where division is not valid.', 'Overflow before modulo.', 'Treating 1 as prime.'],
  },
  stringMatching: {
    idea: 'Preprocess the pattern so a mismatch reuses known prefix/suffix information instead of restarting comparisons.',
    recognize: ['Find pattern occurrences, borders, longest happy prefix, shortest palindrome, or repeated prefix structure.', 'Naive matching may revisit the same text characters.'],
    steps: ['Define the prefix table or Z value precisely.', 'Build preprocessing state while reusing shorter borders.', 'Compare text and pattern pointers.', 'On mismatch, fall back using preprocessing rather than moving both pointers blindly.'],
    invariant: 'The stored prefix value is the longest proper prefix that is also a suffix for the processed pattern prefix.',
    complexity: 'KMP and Z-function are O(n + m); Rabin–Karp is average O(n + m) with collision verification.',
    pitfalls: ['Off-by-one prefix lengths.', 'Using hash equality without collision verification.', 'Moving the text pointer during a reusable KMP fallback.'],
  },
  palindrome: {
    idea: 'Exploit mirrored positions, expand around centers, or preprocess transformed strings for repeated palindrome queries.',
    recognize: ['Longest/shortest palindrome, palindromic counts, or adding characters to form a palindrome.', 'The property is symmetric around a center.'],
    steps: ['Choose two pointers, center expansion, DP, hashing, or Manacher based on constraints.', 'Define odd and even centers.', 'Expand only while boundaries and characters match.', 'Translate transformed indices carefully.'],
    invariant: 'The active radius or pointer pair encloses a confirmed palindrome.',
    complexity: 'Center expansion is O(n²); Manacher is O(n); simple validation is O(n).',
    pitfalls: ['Ignoring even-length palindromes.', 'Incorrect transformed-index conversion.', 'Using advanced linear algorithms when a simpler bound already passes.'],
  },
} satisfies Record<string, PatternTheory>

export type PatternFamily = keyof typeof theories

const blueprints: Record<PatternFamily, PatternBlueprint> = {
  linkedList: {
    state: 'prev, current, next_node, and possibly a dummy node',
    template: ['current = head', 'while current:', '    next_node = current.next', '    # read or rewire current safely', '    prev, current = current, next_node', 'return new_head_or_answer'],
    adapt: ['What links must change?', 'Can a dummy node remove a head special case?', 'Which pointer must be saved before mutation?'],
    trace: ['Pause before each pointer assignment.', 'Point to the still-reachable unprocessed chain.', 'After rewiring, verify no node was lost.'],
  },
  fastSlow: {
    state: 'slow, fast, and the meaning of their relative distance',
    template: ['slow = fast = head', 'while fast and fast.next:', '    slow = slow.next', '    fast = fast.next.next', '    if meeting_condition: ...', 'return result_from_pointer_relationship'],
    adapt: ['Should fast move twice as quickly or start k steps ahead?', 'What does a meeting prove?', 'What termination condition keeps every access safe?'],
    trace: ['Record both positions after a complete iteration.', 'Explain the distance encoded by the pointers.', 'Check odd, even, cyclic, and one-node inputs.'],
  },
  fundamentals: {
    state: 'the current index and the processed result',
    template: ['result = initial_value', 'for index, value in enumerate(values):', '    # apply exactly one required operation', '    update(result, value)', 'return result'],
    adapt: ['What is the simplest correct operation?', 'Does the output require a new collection?', 'What is true after processing index i?'],
    trace: ['Write the state after each iteration.', 'Check the first and final valid index.', 'Separate input storage from auxiliary space.'],
  },
  array: {
    state: 'scan indices plus the smallest running summary needed',
    template: ['state = initial_state', 'for index, value in enumerate(values):', '    update_state(value)', '    if candidate_is_valid:', '        update_answer()', 'return answer'],
    adapt: ['Is the answer about a prefix, suffix, subarray, or rearrangement?', 'Can one running value replace a nested loop?', 'May the input be changed?'],
    trace: ['Mark the processed boundary.', 'State what the running variables summarize.', 'Test duplicates, negatives, and length one.'],
  },
  kadane: {
    state: 'best_ending_here and best_seen',
    template: ['current = best = values[0]', 'for value in values[1:]:', '    current = max(value, current + value)', '    best = max(best, current)', 'return best'],
    adapt: ['Do you need maximum sum, minimum sum, or actual boundaries?', 'What does “ending here” mean?', 'How will all-negative input behave?'],
    trace: ['Compare restart versus extend.', 'Keep current distinct from global best.', 'Track boundaries when the prompt asks for the subarray.'],
  },
  matrix: {
    state: 'row, column, direction, and active boundaries or visited state',
    template: ['initialize row/column bounds', 'while boundaries_are_valid:', '    traverse one direction', '    shrink its boundary', '    guard before traversing the opposite side', 'return result'],
    adapt: ['Is movement directional, neighbor-based, or layer-based?', 'Can markers be stored in the matrix?', 'When do boundaries cross?'],
    trace: ['Write coordinates beside each move.', 'Mark cells exactly once.', 'Test one row, one column, and rectangular matrices.'],
  },
  string: {
    state: 'character indices, counts, or an output buffer',
    template: ['result = []', 'for index, char in enumerate(text):', '    if char_satisfies_rule:', '        result.append(transform(char))', 'return "".join(result)'],
    adapt: ['Is order important?', 'Can two pointers avoid a new string?', 'Which normalization rules are allowed?'],
    trace: ['Show the processed substring.', 'Avoid repeated immutable concatenation.', 'Test empty, repeated, and mixed-case input.'],
  },
  twoPointers: {
    state: 'left, right, and what has been eliminated outside them',
    template: ['left, right = 0, len(values) - 1', 'while left < right:', '    measure = evaluate(values[left], values[right])', '    if goal_reached: return answer', '    if measure_too_small: left += 1', '    else: right -= 1'],
    adapt: ['What ordering makes pointer movement valid?', 'Which movement improves the current measure?', 'Do original indices need preservation?'],
    trace: ['Predict which pointer moves before stepping.', 'Name the candidates discarded by that move.', 'Verify pointer equality and duplicate behavior.'],
  },
  stack: {
    state: 'a stack whose entries have one precise unresolved meaning',
    template: ['stack = []', 'for item in input:', '    while stack and item_resolves(stack[-1]):', '        resolve(stack.pop(), item)', '    if item_needs_future_work:', '        stack.append(item)', 'finish_remaining(stack)'],
    adapt: ['What does one stack entry represent?', 'Is the newest unresolved item the next one to resolve?', 'Do you need values, indices, or pairs?'],
    trace: ['Say why every push is necessary.', 'Say what proof permits every pop.', 'Describe the meaning of the remaining stack.'],
  },
  monotonic: {
    state: 'indices maintained in increasing or decreasing value order',
    template: ['stack = []', 'for index, value in enumerate(values):', '    while stack and value_dominates(values[stack[-1]]):', '        previous = stack.pop()', '        answer[previous] = value_or_distance', '    stack.append(index)'],
    adapt: ['Nearest greater or smaller?', 'Left or right?', 'Should equal values pop?', 'Is a deque needed for window expiry?'],
    trace: ['Check monotonic order after every step.', 'Charge each element for at most one push and pop.', 'Resolve remaining entries with the required default.'],
  },
  hashing: {
    state: 'a set or map summarizing exactly the processed elements',
    template: ['seen_or_counts = {}', 'for index, value in enumerate(values):', '    needed = derive_needed(value)', '    if needed in seen_or_counts:', '        use_previous_information()', '    store_current(value, index)', 'return answer'],
    adapt: ['Membership, frequency, grouping, or index lookup?', 'Query before or after insertion?', 'Can duplicate entries overwrite one another?'],
    trace: ['Display the map before the current update.', 'Explain the key and value semantics.', 'Test repeated and missing keys.'],
  },
  prefix: {
    state: 'current prefix plus a map or array of earlier prefixes',
    template: ['prefix = 0', 'history = {0: 1}', 'for value in values:', '    prefix += value', '    answer += history.get(prefix - target, 0)', '    history[prefix] = history.get(prefix, 0) + 1'],
    adapt: ['Sum, XOR, product, or another associative summary?', 'Do you need one range answer or count many ranges?', 'What empty-prefix value is required?'],
    trace: ['Compute the needed earlier prefix.', 'Query before storing the current prefix.', 'Explain the subarray boundaries created by subtraction.'],
  },
  slidingWindow: {
    state: 'left, right, and state describing only the current window',
    template: ['left = 0', 'for right, value in enumerate(values):', '    add(value)', '    while window_is_invalid:', '        remove(values[left]); left += 1', '    update_answer(left, right)'],
    adapt: ['Fixed or variable length?', 'What makes the window invalid?', 'Does shrinking monotonically restore validity?'],
    trace: ['Verify counts match [left, right].', 'Predict expansion versus shrinking.', 'Update the answer only when its required condition holds.'],
  },
  trie: {
    state: 'current trie node and the processed character or bit prefix',
    template: ['node = root', 'for symbol in key:', '    if symbol not in node.children:', '        node.children[symbol] = Node()', '    node = node.children[symbol]', 'node.is_word = True'],
    adapt: ['Characters or binary bits?', 'Full-word lookup or prefix lookup?', 'What metadata belongs at each node?'],
    trace: ['Spell the root-to-node prefix.', 'Separate missing path from incomplete word.', 'For XOR, justify choosing the opposite bit.'],
  },
  tree: {
    state: 'the contract returned by one subtree',
    template: ['def solve(node):', '    if node is None: return base_value', '    left = solve(node.left)', '    right = solve(node.right)', '    return combine(node, left, right)'],
    adapt: ['What must a parent know from each child?', 'When should the node be processed?', 'Would BFS better represent level order?'],
    trace: ['State the return contract before recursing.', 'Show base-case values.', 'Combine only information promised by child calls.'],
  },
  heap: {
    state: 'a heap of currently eligible candidates and their priorities',
    template: ['heap = []', 'for candidate in initial_candidates:', '    heappush(heap, priority_entry(candidate))', 'while heap:', '    priority, item = heappop(heap)', '    if stale(item): continue', '    add_newly_eligible_candidates(item)'],
    adapt: ['Min or max priority?', 'What makes a candidate eligible?', 'What tie-breaker keeps entries comparable?'],
    trace: ['Inspect only the root guarantee.', 'Explain every push source.', 'Identify and skip stale entries where necessary.'],
  },
  intervals: {
    state: 'sorted intervals plus one active merged/selected boundary',
    template: ['intervals.sort(key=chosen_boundary)', 'result = []', 'for start, end in intervals:', '    if compatible_with_active(start, end):', '        merge_or_select()', '    else:', '        finish_active_and_start_new()'],
    adapt: ['Merge overlaps or maximize selections?', 'Sort by start or finish?', 'Are touching endpoints overlapping?'],
    trace: ['Compare only with the active boundary.', 'Explain why earlier intervals need no revisit.', 'Append or count the final active interval.'],
  },
  sorting: {
    state: 'sorted runs or a boundary separating final and unsorted regions',
    template: ['divide input into smaller runs', 'sort each run', 'merge or place the next correct element', 'maintain the sorted-region invariant', 'return sorted result'],
    adapt: ['Stability required?', 'In-place required?', 'Small value range or arbitrary comparable keys?'],
    trace: ['Mark which region is already sorted.', 'Count comparisons and moves separately.', 'Include sorting in the surrounding solution complexity.'],
  },
  graph: {
    state: 'adjacency list, visited set, and BFS queue or DFS stack',
    template: ['frontier = [start]', 'visited = {start}', 'while frontier:', '    node = remove_next(frontier)', '    for neighbor in graph[node]:', '        if neighbor not in visited:', '            visited.add(neighbor); add(frontier, neighbor)'],
    adapt: ['Directed or undirected?', 'Reachability, levels, components, or paths?', 'BFS shortest unweighted distance or DFS structure?'],
    trace: ['Mark visited when discovered.', 'Show the frontier separately from completed nodes.', 'Restart traversal for disconnected components when required.'],
  },
  topo: {
    state: 'remaining indegrees and a queue of currently prerequisite-free nodes',
    template: ['indegree = compute_indegrees(graph)', 'queue = all_zero_indegree_nodes()', 'while queue:', '    node = queue.popleft(); order.append(node)', '    for neighbor in graph[node]:', '        indegree[neighbor] -= 1', '        if indegree[neighbor] == 0: queue.append(neighbor)'],
    adapt: ['What direction represents prerequisite → dependent?', 'Do you need one order or detect impossibility?', 'Should ties have lexical ordering?'],
    trace: ['Only emit zero-indegree nodes.', 'Explain each indegree decrement.', 'Compare output length with V to detect a cycle.'],
  },
  shortestPath: {
    state: 'best-known distance per node and a frontier ordered by distance',
    template: ['distance = [infinity] * n; distance[source] = 0', 'frontier = [(0, source)]', 'while frontier:', '    cost, node = pop_best(frontier)', '    if cost != distance[node]: continue', '    for neighbor, weight in graph[node]:', '        relax(node, neighbor, weight)'],
    adapt: ['Unweighted, non-negative, negative, or DAG edges?', 'Single source or all pairs?', 'Need path reconstruction?'],
    trace: ['State why the chosen algorithm matches weight constraints.', 'Show every relaxation inequality.', 'Ignore stale heap entries.'],
  },
  unionFind: {
    state: 'parent and size/rank arrays representing component leaders',
    template: ['def find(node):', '    if parent[node] != node:', '        parent[node] = find(parent[node])', '    return parent[node]', 'def union(a, b):', '    root_a, root_b = find(a), find(b)', '    attach_smaller_tree_to_larger()'],
    adapt: ['Are connections only added?', 'Need component count or cycle detection?', 'Which objects map to DSU indices?'],
    trace: ['Find roots before every union.', 'Show path compression changes.', 'Never union a raw child as if it were a root.'],
  },
  backtracking: {
    state: 'partial candidate, next choice position, and constraint bookkeeping',
    template: ['def search(state):', '    if complete(state): record_copy(state); return', '    for choice in valid_choices(state):', '        apply(choice)', '        search(state)', '        undo(choice)'],
    adapt: ['What is one level of choice?', 'Can invalid branches be rejected earlier?', 'Are choices reusable or unique?'],
    trace: ['Say choose, explore, undo aloud.', 'Verify state after undo equals state before choose.', 'Copy mutable answers when recording.'],
  },
  recursion: {
    state: 'function parameters defining one strictly smaller problem',
    template: ['def solve(state):', '    if smallest_case(state): return base_answer', '    smaller_answer = solve(reduce(state))', '    return combine_current_with(smaller_answer)'],
    adapt: ['What exactly does one call promise?', 'How does every call get smaller?', 'Are overlapping states suggesting memoization?'],
    trace: ['Write parameters for every stack frame.', 'Resolve base cases first while unwinding.', 'Count total calls and maximum depth separately.'],
  },
  subsets: {
    state: 'current choices and next available index',
    template: ['def generate(index, path):', '    if index == len(values): record(path.copy()); return', '    generate(index + 1, path)', '    path.append(values[index])', '    generate(index + 1, path)', '    path.pop()'],
    adapt: ['Take/not-take or choose-from-start style?', 'Need combinations of fixed size?', 'How are duplicate values skipped?'],
    trace: ['Draw one binary decision level per item.', 'Distinguish index advancement from path mutation.', 'Verify exactly one undo for each choose.'],
  },
  binarySearch: {
    state: 'low, high, and a monotonic predicate determining the surviving half',
    template: ['low, high = minimum_candidate, maximum_candidate', 'while low <= high:', '    mid = low + (high - low) // 2', '    if feasible(mid):', '        answer = mid; high = mid - 1', '    else:', '        low = mid + 1'],
    adapt: ['Exact value or first/last valid boundary?', 'What predicate changes monotonically?', 'Which side contains the desired boundary after success?'],
    trace: ['Write the surviving answer range.', 'Predict the discarded half.', 'Verify loop progress for two adjacent candidates.'],
  },
  dp: {
    state: 'the minimum variables that uniquely identify a reusable subproblem',
    template: ['define dp[state] in one sentence', 'initialize base states', 'for state in dependency_safe_order:', '    dp[state] = best_or_count(', '        transition_from_smaller_states', '    )', 'return requested_state'],
    adapt: ['What decisions remain?', 'Which parameters change future possibilities?', 'Count, feasibility, minimum, maximum, or reconstruction?'],
    trace: ['Name the meaning of the active cell.', 'Point to every dependency used.', 'Derive tabulation only after memoized recursion is clear.'],
  },
  greedy: {
    state: 'the chosen solution so far and the future flexibility it preserves',
    template: ['candidates.sort(key=greedy_key)', 'answer = []', 'for candidate in candidates:', '    if compatible(candidate, answer):', '        choose(candidate)', 'return answer'],
    adapt: ['What local choice leaves the most future options?', 'Can any optimal solution exchange its first choice for yours?', 'What counterexample would disprove the rule?'],
    trace: ['Explain why each rejected item cannot improve the result.', 'Track the preserved resource or boundary.', 'Do not accept a rule without an exchange argument.'],
  },
  bit: {
    state: 'a mask or accumulator whose individual bits have stated meanings',
    template: ['mask_or_answer = 0', 'for value in values:', '    mask_or_answer = update_with_bit_operation(value)', 'return decode(mask_or_answer)'],
    adapt: ['Does XOR cancellation help?', 'Does each subset correspond to a mask?', 'Which bit must be tested, set, cleared, or toggled?'],
    trace: ['Write operands in binary.', 'Evaluate one bit position independently.', 'State integer-width and sign assumptions.'],
  },
  math: {
    state: 'the reduced numeric quantities required by the chosen identity',
    template: ['identify mathematical invariant or identity', 'reduce the input using gcd/sieve/modulo/factors', 'iterate only over necessary candidates', 'maintain safe arithmetic', 'return transformed result'],
    adapt: ['Divisibility, primes, gcd, modular power, or counting?', 'Can symmetry reduce work to √n?', 'Where can overflow occur?'],
    trace: ['Show why every transformation preserves the answer.', 'Check 0 and 1 explicitly.', 'Apply modulo after safe operations.'],
  },
  stringMatching: {
    state: 'pattern preprocessing plus text and pattern indices',
    template: ['prefix = build_prefix_table(pattern)', 'text_index = pattern_index = 0', 'while text_index < len(text):', '    if characters_match: advance_both()', '    elif pattern_index:', '        pattern_index = prefix[pattern_index - 1]', '    else: text_index += 1'],
    adapt: ['One match, all matches, borders, or palindrome construction?', 'KMP, Z, or rolling hash?', 'How is fallback information defined?'],
    trace: ['Build preprocessing before searching.', 'On mismatch, state what matched prefix is reusable.', 'Move the text pointer only when no prefix remains.'],
  },
  palindrome: {
    state: 'left/right boundaries or a center and confirmed radius',
    template: ['for each possible center:', '    left, right = center_boundaries', '    while in_bounds and text[left] == text[right]:', '        update_answer(left, right)', '        left -= 1; right += 1'],
    adapt: ['Validation, longest, count, construction, or many queries?', 'Odd and even centers?', 'Do constraints require linear-time preprocessing?'],
    trace: ['Treat odd and even centers separately.', 'Expand only after equality.', 'Translate any transformed-string positions carefully.'],
  },
}

const patternFamilies: Record<string, PatternFamily> = {
  'Doubly Linked Lists': 'linkedList',
  'Fast and Slow Pointer': 'fastSlow',
  'Linked List Fundamentals': 'linkedList',
  'Linked List Merge and Transformation': 'linkedList',
  'Linked List Pointer Rewiring': 'linkedList',
  'Programming and Complexity Basics': 'fundamentals',
  'Python Collections': 'fundamentals',
  'Array Fundamentals': 'array',
  'Kadane and Running State': 'kadane',
  'Matrix Traversal and Transformation': 'matrix',
  'String Manipulation and Palindromes': 'string',
  'String Parsing and Transformation': 'string',
  'Two Pointers and Hashing': 'twoPointers',
  'Expression Parsing with Stacks': 'stack',
  'Monotonic Stack and Queue': 'monotonic',
  'Stack and Queue Fundamentals': 'stack',
  'Stack, Queue and Cache Design': 'stack',
  'Array Hashing and Counting': 'hashing',
  'Hashing and Frequency Counting': 'hashing',
  'Prefix Sum and Subarray Counting': 'prefix',
  'Sliding Window and Two Pointers': 'slidingWindow',
  'String Hashing and Counting': 'hashing',
  'Bitwise Tries': 'trie',
  'Tree Construction, Paths and Serialization': 'tree',
  'Tree Traversals': 'tree',
  'Tree Views and Properties': 'tree',
  'Trie and Prefix Search': 'trie',
  'Heap Fundamentals': 'heap',
  'Heap and Priority Queue Patterns': 'heap',
  Intervals: 'intervals',
  'Sorting Algorithms': 'sorting',
  'Advanced Graph Algorithms': 'graph',
  'Graph BFS and DFS': 'graph',
  'Graph Fundamentals': 'graph',
  'Minimum Spanning Tree and Union Find': 'unionFind',
  'Shortest Path Algorithms': 'shortestPath',
  'Topological Sort and DAGs': 'topo',
  'Backtracking and Constraint Search': 'backtracking',
  'Recursion Fundamentals': 'recursion',
  'Recursive Problem Solving': 'recursion',
  'Subsets and Subsequences': 'subsets',
  'Binary Search on Answer': 'binarySearch',
  'Binary Search on Matrices': 'binarySearch',
  'Binary Search on Sorted Data': 'binarySearch',
  'DP on Rectangles and Squares': 'dp',
  'Dynamic Programming Fundamentals': 'dp',
  'Grid and Multi-Dimensional DP': 'dp',
  'Knapsack and Subsequence DP': 'dp',
  'Longest Increasing Subsequence': 'dp',
  'One-Dimensional DP': 'dp',
  'Partition DP': 'dp',
  'State Machine DP': 'dp',
  'String DP': 'dp',
  'Advanced Math': 'math',
  'Bit Manipulation': 'bit',
  'Greedy Choice': 'greedy',
  'Greedy Interval Scheduling': 'greedy',
  'Math and Number Theory': 'math',
  'Advanced Palindrome Algorithms': 'palindrome',
  'String Matching Algorithms': 'stringMatching',
}

export function getPatternTheory(pattern: string): PatternTheory {
  return theories[patternFamilies[pattern] ?? 'fundamentals']
}

export function getPatternBlueprint(pattern: string): PatternBlueprint {
  return blueprints[patternFamilies[pattern] ?? 'fundamentals']
}

export function getPatternFamily(pattern: string): PatternFamily {
  return patternFamilies[pattern] ?? 'fundamentals'
}
